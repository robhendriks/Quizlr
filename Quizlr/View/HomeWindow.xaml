<Window x:Class="Quizlr.View.HomeWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quizlr"
        Height="300" Width="300"
        WindowStartupLocation="CenterScreen"
        Style="{StaticResource BasicWindow}"
        DataContext="{Binding Home, Source={StaticResource Locator}}">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!--Manage-->
        <StackPanel Grid.Row="0">
            <Label Content="Beheer"/>
            <StackPanel Orientation="Horizontal">
                <Button Content="Quizzen" Command="{Binding OpenQuizzesCommand}" />
                <Button Content="Vragen" Command="{Binding OpenQuestionsCommand}" />
            </StackPanel>
        </StackPanel>
        <!--/Manage-->
        
        <!--Play-->
        
        <!--/Play-->
    </Grid>
</Window>